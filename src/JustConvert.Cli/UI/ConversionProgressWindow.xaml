<ui:FluentWindow x:Class="JustConvert.Cli.UI.ConversionProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:core="clr-namespace:JustConvert.Core;assembly=JustConvert.Core"
        Height="210" Width="480"
        MinHeight="210" MaxHeight="210"
        MinWidth="480" MaxWidth="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <ui:FluentWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Light" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" x:Name="AppTitleBar" ShowMaximize="False" ShowMinimize="True" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Height="36"/>

        <Grid Grid.Row="1" x:Name="ProgressPanel" Margin="24,8,24,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <ui:SymbolIcon x:Name="IconProgress" Grid.Column="0" Symbol="ArrowSync24" FontSize="24" Margin="0,0,12,0" VerticalAlignment="Center" Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" RenderTransformOrigin="0.5,0.5">
                    <ui:SymbolIcon.RenderTransform>
                        <RotateTransform x:Name="IconRotateTransform" Angle="0"/>
                    </ui:SymbolIcon.RenderTransform>
                </ui:SymbolIcon>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock x:Name="TxtFileName" FontWeight="SemiBold" FontSize="14" TextTrimming="CharacterEllipsis" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                    <TextBlock x:Name="TxtTargetFormat" FontSize="12" Margin="0,2,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Margin="0,0,0,4">
                <TextBlock x:Name="TxtStatus" Text="{core:Loc StatusPreparing}" FontSize="12" Foreground="{DynamicResource TextFillColorPrimaryBrush}" HorizontalAlignment="Left"/>
                <TextBlock x:Name="TxtProgressDetails" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" HorizontalAlignment="Right"/>
            </Grid>

            <ProgressBar Grid.Row="2" x:Name="ProgressBar" IsIndeterminate="True" Height="4" Foreground="{DynamicResource AccentFillColorDefaultBrush}" Margin="0,4,0,0"/>

            <Grid Grid.Row="3" Margin="0,16,0,0" VerticalAlignment="Bottom">
                <ui:Button x:Name="BtnCancel" Content="{core:Loc BtnCancel}" Click="BtnCancel_Click" HorizontalAlignment="Right" MinWidth="85"/>
            </Grid>
        </Grid>

        <Grid Grid.Row="1" x:Name="ErrorPanel" Visibility="Collapsed" Margin="24,4,24,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ui:InfoBar Grid.Row="0" x:Name="InfoBarError" Title="{core:Loc TitleError}" IsOpen="True" Severity="Error" IsClosable="False" Margin="0,0,0,12"/>

            <ui:TextBox Grid.Row="1" x:Name="TxtErrorLog" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" FontFamily="Consolas, monospace" FontSize="11" Padding="8"/>

            <Border Grid.Row="2" Background="Transparent" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="0,1,0,0" Padding="0,12,0,0" Margin="0,12,0,0">
                <Grid>
                    <ui:Button x:Name="BtnCopy" Content="{core:Loc BtnCopyError}" Click="BtnCopy_Click" HorizontalAlignment="Left" MinWidth="110"/>
                    <ui:Button x:Name="BtnClose" Content="{core:Loc BtnClose}" Click="BtnClose_Click" IsDefault="True" IsCancel="True" HorizontalAlignment="Right" MinWidth="80"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</ui:FluentWindow>
